schema_version = "1.3"
artifacts = ["/logs/artifacts/model.patch"]
[task]
name = "benzhi/flask-cli-config-command"
description = ""
authors = []
keywords = []
[metadata]
ext_id = "kh2cc1e830b6f0ac95ad44116b903929"
task_id = "flask-cli-config-command"
display_title = "Add a flask config CLI command"
display_description = "Show the application's effective configuration from the Flask CLI, with key filtering, sorted output, and a JSON mode, running inside the app context."
original_title = "flask config 命令显示有效配置"
category = "feature_request"
language = "python"
repository_url = "https://github.com/pallets/flask"
base_commit_hash = "d318b683471101618febed18996405ad26462110"
[verifier]
network_mode = "no-network"
environment_mode = "separate"
timeout_sec = 1800.0

[verifier.env]
[verifier.environment]
build_timeout_sec = 1800.0
cpus = 2
memory_mb = 8192
storage_mb = 20480

[[verifier.collect]]
command = "cd /app && mkdir -p /logs/artifacts && git config --global --add safe.directory /app && git diff --binary d318b683471101618febed18996405ad26462110 HEAD > /logs/artifacts/model.patch"
timeout_sec = 300.0

[agent]
network_mode = "no-network"
timeout_sec = 10800.0
[environment]
build_timeout_sec = 1800.0
# docker_image is omitted in step 1: Harbor/Pier builds environment/Dockerfile.
# After the image is built and pushed, set e.g.
# docker_image = "<registry>/benzhi-swe:kh2cc1e830b6f0ac95ad44116b903929-v1"
os = "linux"
cpus = 2
memory_mb = 8192
storage_mb = 20480
gpus = 0
mcp_servers = []

[environment.env]
[solution.env]
